name: "\U0001F3CE️ Swift Build| WrkstrmMain"
on:
  push:
    branches:
    - main
    paths:
    - '**'
  pull_request:
    branches:
    - main
jobs:
  swift-build:
    strategy:
      matrix:
        os:
        - macos-13
        - ubuntu-latest
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive
    - name: Cache Swift PM dependencies
      id: cache-spm
      uses: actions/cache@v2
      with:
        restore-keys: ${{ runner.os }}-spm-WrkstrmMain-
        path: apple/WrkstrmMain/.build
        key: ${{ runner.os }}-spm-WrkstrmMain-${{ hashFiles('**/Package.resolved')
          }}
    - name: Check cache
      run: 'echo ''Cache hit: ${{ steps.cache-spm.outputs.cache-hit }}'''
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      if: matrix.os == 'macos-latest' || matrix.os == 'macos-13' || matrix.os == 'self-hosted'
      with:
        xcode-version: '15.0'
    - name: Swift Describe
      if: steps.cache-spm.outputs.cache-hit != 'true'
      run: swift package describe --type json
    - name: Swift Build
      run: swift build --target WrkstrmMain
