// Foundation-free typealias for a JSON object.

extension JSON {
  /// Dictionary with `String` keys and `Any` values, used as a flexible JSON object shape.
  public typealias AnyDictionary = [String: Any]
}
